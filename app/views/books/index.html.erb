
<h1>Books</h1>

<% @books.each do |book| %>

  <p>タイトル</p>

<% end %>

<%= form_with model: @book, url: '/books', method: :post do |f| %>

<h4>New book</h4>

<h4>Title</h4>
<%= f.text_field :title %>

<h4>Body</h4>
<%= f.text_area :body %>

<%= f.submit 'Create Book' %>

<% end %>